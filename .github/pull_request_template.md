## Summary

## Checklist

- [ ] Tests added or updated; `pytest` passes without network access
- [ ] `ruff check src tests` passes
- [ ] User- and model-visible text is English (`tests/test_english_text.py`)
- [ ] `CHANGELOG.md` updated for user-visible changes
- [ ] `python -m build && twine check --strict dist/*` passes when packaging changed
